export class Ingredient {
    // Alternative way
    // public name: string;
    // public amount: number;

    constructor(public name: string, public amount: number){
        // No need this if using above way
        // this.name = name;
        // this.amount = amount;
    }
}